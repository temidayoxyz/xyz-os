name: Code mode
description: All Standard capabilities, plus the Code Mode SDK — the model composes multi-step operations as a TypeScript program. Best for programming, building, and shipping.
order: 2
